{
  "name": "lynnhub-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "LynnHub 桌面端 - Tauri 壳 + 复用 Web UI + HermesAgent 本地进程",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "frontend:dev": "cd ../ && npx next dev -p 5176",
    "frontend:build": "cd ../ && npx next build",
    "sidecar:dev": "node sidecar/dev-agent.js",
    "sidecar:build": "node sidecar/build-agent.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-shell": "^2.0.1",
    "@tauri-apps/plugin-fs": "^2.0.1",
    "@tauri-apps/plugin-dialog": "^2.0.1",
    "@tauri-apps/plugin-updater": "^2.0.1",
    "@tauri-apps/plugin-process": "^2.0.1",
    "@tauri-apps/plugin-http": "^2.0.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "ws": "^8.18.0"
  }
}
