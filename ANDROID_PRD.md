# Lynx 安卓端 PRD 方案

> 本文档定义 Lynx 安卓 APP 的产品定位、功能取舍、远程操控 PC/Web Agent 方案、信息架构与设计规范。

## 1. 产品定位

### 1.1 一句话定位
**Lynx 安卓端 = 便携指挥台** —— 出门在外也能捕捉灵感、查看决策、对话 AI 助理、远程指挥 PC 桌面端 / Web 端的 Lynx Agent 执行任务。

### 1.2 核心使用场景
| 场景 | 频次 | 关键动作 |
|------|------|----------|
| 通勤路上灵感捕捉 | 高 | 闪电输入 / 语音速记 |
| 碎片时间查看决策 | 高 | 看板浏览 / 任务状态更新 |
| 移动场景对话 AI | 高 | AI 助理 SSE 流式对话 |
| 远程启动 PC Agent | 中 | 一键唤醒桌面端 Lynx Agent |
| 外出时下发指令 | 中 | "帮我跑一下今天的巡检" |
| 接收 PC 任务完成通知 | 中 | WS 推送 / 飞书加急 |
| 管理员应急处理 | 低 | 用户禁用 / 角色调整 |

### 1.3 与其他端的关系
- **Web 端**：全功能工作站（编排 / 配置 / 备份 / 管理）
- **桌面端**：本地 Lynx Agent 引擎 + 重计算 + RPA
- **安卓端**：便携 + 核心 + 远程指挥（**不做重操作**）

## 2. 功能取舍（DO / DON'T）

### 2.1 ✅ DO — 必须做（便携 + 核心 + 远程操控）

#### A. 灵感管理（核心便携）
- ✅ 闪电灵感捕捉（文本 / 语音速记 / 图片附件）
- ✅ 灵感列表查看 / 搜索 / 编辑
- ✅ 灵感收敛决策（归位看板 / 送入墓地）
- ✅ 灵感墓地查看 / 复活检查

#### B. 决策看板（核心查看）
- ✅ 三层看板查看（北极星 / 战役 / 任务）
- ✅ 任务状态切换（active / done）
- ✅ 任务详情查看（含 AI 提取的认知）
- ✅ 简单拖拽排序（手势支持）
- ✅ 回收站查看 / 恢复

#### C. AI 超级助理（核心交互）
- ✅ SSE 流式对话（首字 < 800ms）
- ✅ 多会话切换
- ✅ 消息 good / bad 标注反馈
- ✅ 语音输入（ASR）+ 语音朗读（TTS）
- ✅ 工具调用结果可视化
- ✅ 图片附件发送（视觉多模态）

#### D. 远程操控 Lynx Agent（差异化核心）
- ✅ **查看 Agent 在线状态**：哪些设备在线（PC / Web）
- ✅ **远程启动 / 停止 Agent**：一键唤醒桌面端 Lynx Agent
- ✅ **下发自然语言指令**：如"帮我整理今天的灵感" / "跑一下巡检"
- ✅ **查看 Agent 任务队列**：正在做什么 / 排队中 / 已完成
- ✅ **任务结果回看**：Agent 执行完成的截图 / 文本 / 数据
- ✅ **操作审批**：高危操作手机端弹窗批准 / 拒绝
- ✅ **Agent 报告订阅**：主动汇报通过 WS 推送到手机

#### E. 任务管理（轻量）
- ✅ 飞书任务列表查看 / 状态更新
- ✅ 任务创建（简易表单）
- ✅ 任务同步状态

#### F. 记忆 / 认知（查看为主）
- ✅ 记忆图谱可视化（力导向图，支持缩放）
- ✅ 认知库浏览 / 搜索
- ✅ 对话资产查看

#### G. 通知中心（便携必备）
- ✅ Web Push 接收
- ✅ 飞书加急消息接收
- ✅ Lynx Agent 主动汇报推送

#### H. 个人设置（基础）
- ✅ 个人资料修改
- ✅ 主题切换（浅 / 深 / 系统）
- ✅ AI Key 配置（用户级）
- ✅ 登出 / 修改密码

### 2.2 ❌ DON'T — 不做（重操作 / 低频 / 复杂）

#### A. 工作流编排（复杂）
- ❌ 可视化工作流编辑器（节点连线复杂，桌面更适合）
- ❌ 工作流定时配置
- ✅ *替代方案*：仅支持查看工作流列表 + 手动触发执行

#### B. 技能管理（重）
- ❌ 技能 SSE 流式生成（长耗时，桌面更适合）
- ❌ 技能导入 / 导出
- ❌ 技能市场管理（上传 / 审核）
- ✅ *替代方案*：仅支持查看技能库 + 收藏 + 触发已学技能

#### C. 备份 / 导入（重数据）
- ❌ 全量备份导出
- ❌ 备份文件导入
- ❌ 流式 JSON 导出
- ✅ *替代方案*：仅支持查看备份历史 + 触发云端备份

#### D. 巡检配置（admin 专属）
- ❌ 巡检规则编辑（复杂表单）
- ❌ 巡检 notifyChannels 配置
- ✅ *替代方案*：仅支持查看规则列表 + 手动触发巡检

#### E. 管理后台（低频重操作）
- ❌ 用户管理（创建 / 禁用 / 删除）
- ❌ 角色权限编辑（35 项权限矩阵）
- ❌ 词元统计排行榜（桌面查看更合适）
- ❌ 职业空间配置
- ✅ *替代方案*：仅支持查看自己的词元消耗 + 管理员应急禁用用户

#### F. 系统设置（复杂）
- ❌ 桌面端 HermesAgent 配置（授权目录 / 模型选择）
- ❌ 远程操控 WS 网关配置
- ❌ 飞书 Webhook 配置
- ❌ Web Push VAPID 密钥配置
- ❌ 诊断工具
- ✅ *替代方案*：仅支持查看连接状态 + 一键重连

#### G. 桌面端专属功能
- ❌ RPA 自动化操作
- ❌ 本地文件系统访问
- ❌ 桌面截图 / 屏幕共享
- ❌ 全局快捷键

## 3. 信息架构

### 3.1 五 Tab 结构

```
┌─────────────────────────────────────────┐
│  聚焦   看板   助理   任务   我的        │
└─────────────────────────────────────────┘
```

| Tab | 名称 | 核心内容 |
|-----|------|----------|
| 1 | 聚焦 | 今日待办 + 灵感速记 + Agent 状态卡 + 快捷入口 |
| 2 | 看板 | 三层看板（北极星 / 战役 / 任务） + 回收站 |
| 3 | 助理 | AI 对话（SSE 流式）+ 语音 + Lynx Agent 远程指令 |
| 4 | 任务 | 飞书任务列表 + 创建 |
| 5 | 我的 | 个人资料 + 词元统计 + 设置 + Agent 设备管理 |

### 3.2 二级页面

- **聚焦 → 灵感速记**（全屏输入）
- **聚焦 → Agent 指令中心**（远程操控面板）
- **看板 → 任务详情**
- **看板 → 回收站**
- **助理 → 会话历史**
- **助理 → Agent 任务队列**
- **助理 → 操作审批**
- **我的 → 词元统计**
- **我的 → Agent 设备管理**
- **我的 → 设置**
- **我的 → 记忆图谱**（也可放聚焦）

### 3.3 关键入口设计

#### 聚焦页（首页）
```
┌─────────────────────────────────┐
│  🌅 早上好，Lynn                │
│  今天有 3 个任务 · 5 条灵感待收敛 │
├─────────────────────────────────┤
│  ⚡ 闪电灵感                    │  ← 顶部主按钮
│  [点击或语音输入]                │
├─────────────────────────────────┤
│  🤖 Lynx Agent 状态             │
│  ┌─────┐  ┌─────┐               │
│  │ PC  │  │ Web │               │
│  │在线 │  │离线 │               │
│  └─────┘  └─────┘               │
│  [下发指令]  [查看队列]          │
├─────────────────────────────────┤
│  📋 今日任务                    │
│  • 任务1 [进行中]               │
│  • 任务2 [已完成]               │
├─────────────────────────────────┤
│  💡 待收敛灵感 (5)              │
│  • 灵感1                        │
│  • 灵感2                        │
└─────────────────────────────────┘
```

#### 助理页 - Agent 指令中心（核心差异化）
```
┌─────────────────────────────────┐
│  🤖 Lynx Agent 指令中心         │
├─────────────────────────────────┤
│  在线设备                       │
│  ┌─────────────────────────┐    │
│  │ 💻 Lynn-PC (Windows)    │    │
│  │ Agent: 运行中            │    │
│  │ 当前任务: 整理灵感(3/12) │    │
│  │ [下发指令] [查看详情]    │    │
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │ 🌐 Web 端               │    │
│  │ Agent: 未启动            │    │
│  │ [远程启动]              │    │
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│  快捷指令                       │
│  [整理今日灵感] [跑巡检]         │
│  [生成日报] [同步飞书]           │
├─────────────────────────────────┤
│  任务队列                       │
│  ▶ 正在执行: 整理灵感           │
│  ⏸ 排队中: 跑巡检              │
│  ✅ 已完成: 同步飞书 (10:32)    │
├─────────────────────────────────┤
│  待审批 (2)                     │
│  ⚠ 删除文件 xxx.md [批准][拒绝] │
└─────────────────────────────────┘
```

## 4. 远程操控 PC/Web Agent 方案

### 4.1 技术架构

```
┌──────────────┐         WS          ┌──────────────┐
│  安卓 APP    │ ←─────────────────→ │  WS 网关     │
│              │   (端口 3001)       │  (独立进程)  │
│  - 指令下发  │                     │              │
│  - 状态订阅  │                     └──────┬───────┘
│  - 审批回调  │                            │
└──────────────┘                            │ TCP
                                     ┌──────┴───────┐
                                     │  PC 桌面端   │
                                     │  Lynx Agent  │
                                     │  (Tauri)     │
                                     └──────────────┘
```

### 4.2 指令协议

#### 安卓 → WS 网关 → PC Agent
```json
{
  "type": "agent.command",
  "deviceId": "pc-xxx",
  "command": "natural_language",
  "input": "帮我整理今天的灵感，归位到看板",
  "priority": "normal",
  "timeout": 60000,
  "taskId": "uuid-xxx"
}
```

#### PC Agent → WS 网关 → 安卓
```json
{
  "type": "agent.status",
  "deviceId": "pc-xxx",
  "taskId": "uuid-xxx",
  "status": "running | completed | failed | approval_required",
  "progress": 0.35,
  "message": "正在处理第 4/12 条灵感",
  "result": null,
  "approval": null
}
```

#### 审批请求
```json
{
  "type": "agent.approval",
  "deviceId": "pc-xxx",
  "taskId": "uuid-xxx",
  "action": "delete_file",
  "target": "D:/temp/old.md",
  "risk": "high",
  "expiresAt": "2026-06-27T15:00:00Z"
}
```

### 4.3 指令类型

| 类型 | 示例 | 风险等级 | 是否需审批 |
|------|------|----------|-----------|
| 查询类 | "今天有多少灵感" | 低 | 否 |
| 整理类 | "归位灵感到看板" | 中 | 否 |
| 巡检类 | "跑一下今日巡检" | 中 | 否 |
| 通知类 | "生成日报并发飞书" | 中 | 否 |
| 文件操作 | "删除 xxx.md" | 高 | ✅ 是 |
| 系统操作 | "关闭浏览器" | 高 | ✅ 是 |
| 批量操作 | "清空回收站" | 高 | ✅ 是 |

### 4.4 安全机制
1. **设备绑定**：首次使用需在 PC 端扫码授权配对
2. **指令签名**：每条指令带 JWT 签名
3. **审批超时**：5 分钟未审批自动拒绝
4. **操作日志**：所有指令记录到 agent-audit 表
5. **紧急停止**：手机端红色按钮，立即停止 PC Agent 所有任务

## 5. 设计规范

### 5.1 视觉风格
- **参考豆包 APP**：简洁、轻量、卡片化
- **主色**：`#0F62FE`（深海宇宙蓝）
- **强调色**：`#00B8D4`（科技青）
- **背景**：`#F5F5F7`（浅色） / `#030816`（深色）
- **字体**：PingFang SC / 系统默认
- **圆角**：12-16px

### 5.2 组件规范

#### 卡片
```vue
<view class="card">
  <view class="card-title">标题</view>
  <view class="card-body">内容</view>
</view>
```
```css
.card {
  background: #ffffff;
  border-radius: 16px;
  padding: 16px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.04);
  margin-bottom: 12px;
}
```

#### 主按钮
```css
.btn-primary {
  background: linear-gradient(135deg, #0F62FE 0%, #00B8D4 100%);
  color: white;
  border-radius: 12px;
  padding: 12px 24px;
  font-size: 14px;
  font-weight: 600;
  box-shadow: 0 4px 12px rgba(15,98,254,0.3);
}
```

#### TabBar
- 自定义 TabBar（已有）
- 选中色：`#0F62FE`（替代当前的 `#f59e0b`）
- 未选中：`#86868b`

### 5.3 交互规范
- **下拉刷新**：所有列表页支持
- **上拉加载**：分页列表支持
- **左滑删除**：列表项支持
- **长按菜单**：复制 / 删除 / 收藏
- **手势返回**：原生支持
- **骨架屏**：加载态显示

### 5.4 动效
- 页面切换：slide（200ms）
- 卡片入场：fade + translateY（300ms）
- 按钮点击：scale(0.96)（100ms）
- 列表项删除：slide-out（200ms）

## 6. 页面清单

| 页面 | 路径 | 优先级 | 状态 |
|------|------|--------|------|
| 登录 | pages/login/login | P0 | ✅ 已有 |
| 聚焦（首页） | pages/index/index | P0 | ✅ 已有（需增强 Agent 状态卡） |
| 看板 | pages/board/board | P0 | ✅ 已有 |
| AI 助理 | pages/ai/chat/chat | P0 | ✅ 已有 |
| 飞书任务 | pages/tasks/tasks | P0 | ✅ 已有 |
| 我的 | pages/settings/settings | P0 | ✅ 已有 |
| 灵感收件箱 | pages/inbox/inbox | P0 | ✅ 已有 |
| 记忆认知 | pages/memory/memory | P1 | ✅ 已有 |
| **Agent 指令中心** | pages/agent/control | P0 | 🆕 需新建 |
| **Agent 任务队列** | pages/agent/queue | P1 | 🆕 需新建 |
| **操作审批** | pages/agent/approval | P0 | 🆕 需新建 |
| **Agent 设备管理** | pages/agent/devices | P1 | 🆕 需新建 |
| 任务详情 | pages/tasks/detail | P1 | 🆕 需新建 |
| 词元统计 | pages/stats/tokens | P2 | 🆕 需新建 |
| 个人资料 | pages/profile/index | P2 | 🆕 需新建 |
| 设置 | pages/settings/index | P2 | 🆕 需新建 |

## 7. API 接入

### 7.1 现有 API（直接复用）
- `POST /api/auth/token` — 登录
- `GET/POST /api/ideas` — 灵感
- `GET/PATCH/DELETE /api/tasks/:id` — 任务
- `GET/POST /api/conversations` — 对话
- `POST /api/ai/chat` — AI 对话（SSE）
- `POST /api/ai/asr` — 语音识别
- `POST /api/ai/tts/stream` — 语音合成
- `GET /api/memory` — 记忆图谱
- `GET /api/cognitions` — 认知库
- `GET /api/lark-tasks` — 飞书任务
- `GET /api/user/profile` — 个人资料
- `GET /api/user/ai-keys` — AI Key 配置

### 7.2 新增 API（远程操控 Agent）
- `GET /api/hermes/status` — 查询所有设备 Agent 状态
- `POST /api/hermes/remote-command` — 下发自然语言指令
- `GET /api/hermes/queue` — 查询任务队列
- `POST /api/hermes/approval/:taskId` — 审批操作
- `POST /api/hermes/emergency-stop` — 紧急停止
- `WS /ws` — 实时状态推送

### 7.3 鉴权
- Bearer Token（`Authorization: Bearer <jwt>`）
- 通过 `POST /api/auth/token` 获取
- Token 存储在 `uni.setStorageSync`
- 401 时自动跳转登录

## 8. 性能指标

| 指标 | 目标 |
|------|------|
| 启动时间 | ≤ 2s |
| 首屏渲染 | ≤ 1s |
| AI 首字延迟 | ≤ 800ms |
| 列表滚动 | 60fps |
| WS 重连 | 5 秒指数退避，最多 10 次 |
| 离线缓存 | 灵感 / 任务本地缓存 24h |
| 包体积 | ≤ 25MB |

## 9. 离线策略
- 灵感 / 任务 / 对话历史本地缓存（SQLite / Storage）
- 离线创建的灵感标记为 `pending_sync`，联网后自动同步
- AI 对话离线时引导用户连接网络
- Agent 远程操控必须在线（实时性要求）

## 10. 通知策略

### 10.1 推送通道
- **Web Push**：通过 VAPID 密钥推送到 PWA（安卓 Chrome）
- **APP 本地推送**：uni-app plus.push.createMessage
- **飞书加急**：通过飞书机器人推送到飞书 APP

### 10.2 通知类型
| 类型 | 触发 | 优先级 |
|------|------|--------|
| Lynx Agent 任务完成 | Agent 完成指令 | 中 |
| Lynx Agent 待审批 | Agent 请求高危操作审批 | 高 |
| 灵感收敛提醒 | 23:00 未处理完 | 中 |
| 巡检报告 | 巡检完成 | 低 |
| 飞书任务分配 | 飞书 webhook | 中 |
| 系统通知 | 管理员公告 | 低 |

## 11. 开发优先级

### Phase 1（MVP，2 周）
- 登录 / 聚焦 / 看板 / AI 助理 / 任务 / 我的（已有）
- 增强：聚焦页 Agent 状态卡
- 新建：Agent 指令中心（远程下发指令 + 状态查看）
- 新建：操作审批页

### Phase 2（增强，2 周）
- 新建：Agent 任务队列页
- 新建：Agent 设备管理页
- 增强：记忆图谱移动端可视化
- 增强：词元统计页

### Phase 3（优化，1 周）
- 离线缓存策略
- 推送通知集成
- 性能优化
- 深色模式支持

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| WS 连接不稳定 | 指令丢失 | 指令持久化 + 重试 + 幂等 |
| Agent 离线 | 指令无法执行 | 离线提示 + 队列缓存 + 上线后执行 |
| 审批超时 | 任务卡住 | 5 分钟超时自动拒绝 + 通知 |
| 误操作 | 数据丢失 | 高危操作二次确认 + 操作日志可回溯 |
| 网络延迟 | 体验差 | 乐观更新 + Loading 占位 + 失败回滚 |

---

> 本 PRD 为安卓端产品契约，开发需严格遵循功能取舍。新增功能需在此文档登记评审。
